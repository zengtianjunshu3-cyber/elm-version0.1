<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.neusoft.elmboot.mapper.OrdersMapper">

    <resultMap type="com.neusoft.elmboot.po.Orders" id="ordersResultMap">
        <id column="orderId" property="orderId"/>
        <result column="userId" property="userId"/>
        <result column="businessId" property="businessId"/>
        <result column="orderDate" property="orderDate"/>
        <result column="orderTotal" property="orderTotal"/>
        <result column="daId" property="daId"/>
        <result column="orderState" property="orderState"/>
        <association property="business" javaType="com.neusoft.elmboot.po.Business"
                     select="com.neusoft.elmboot.mapper.BusinessMapper.getBusinessById" column="businessId"/>
        <association property="deliveryAddress" javaType="com.neusoft.elmboot.po.DeliveryAddress"
                     select="com.neusoft.elmboot.mapper.DeliveryAddressMapper.getDeliveryAddressById" column="daId"/>
        <collection property="list" ofType="com.neusoft.elmboot.po.OrderDetailet"
                    select="com.neusoft.elmboot.mapper.OrderDetailetMapper.listOrderDetailetByOrderId" column="orderId"/>
    </resultMap>

    <insert id="saveOrders" parameterType="com.neusoft.elmboot.po.Orders" useGeneratedKeys="true" keyProperty="orderId">
        insert into orders(userId,businessId,orderDate,orderTotal,daId,orderState)
        values(#{userId},#{businessId},#{orderDate},#{orderTotal},#{daId},0)
    </insert>

    <select id="getOrdersById" parameterType="Integer" resultMap="ordersResultMap">
        select * from orders where orderId=#{orderId}
    </select>

    <select id="listOrdersByUserId" parameterType="String" resultMap="ordersResultMap">
        select * from orders where userId=#{userId} order by orderId
    </select>

    <update id="updateOrdersState" parameterType="com.neusoft.elmboot.po.Orders">
        update orders
        <set>
            <if test="orderState!=null">orderState=#{orderState},</if>
            <if test="orderDate!=null">orderDate=#{orderDate},</if>
            <if test="orderTotal!=null">orderTotal=#{orderTotal},</if>
            <if test="usedRedEnvelope!=null">usedRedEnvelope=#{usedRedEnvelope},</if>
        </set>
        where orderId=#{orderId}
    </update>

    <delete id="removeOrders" parameterType="Integer">
        delete from orders where orderId=#{orderId}
    </delete>

</mapper>
